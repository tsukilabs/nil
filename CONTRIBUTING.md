# Contributing

## Project Structure

- `nil/src`
  - `components`: Components used by the entire application. Any scene-specific components should be placed in the scene's folder.
  - `scenes`: The different pages of the application. Each scene should have its own folder.
